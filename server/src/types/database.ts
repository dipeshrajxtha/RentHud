/**
 * RentHub Core Database Type Definitions
 * Kysely Schema Contract
 * 
 * Note: Business tables (users, properties, tenancies, etc.) will be added in subsequent phases.
 * Currently establishes foundational schema interface.
 */

// Placeholder empty schema or foundational migrations table
export interface Database {
  // Foundational schema interface - domain tables will be added in feature phases
  [key: string]: any;
}
